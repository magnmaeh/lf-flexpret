target C {
    single-threaded: true,
    platform: "FlexPRET"
};

preamble {=
    #include <flexpret/io.h>
=}

main reactor {
    reaction(startup) {=
        printf("Hello world from test.lf: %i\n", 0x00000001);
    =}
}
